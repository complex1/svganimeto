/**
 * Preview uses the same timeline + resolveTransform path as the editor (no separate WAAPI layer in MVP 1).
 * Future: optional Web Animations API for smoother compositing.
 */
export const previewEngineVersion = 1
